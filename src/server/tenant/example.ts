import "server-only";

/**
 * Placeholder for tenant-surface server logic (e.g. unit token
 * validation — see spec/screens/tenant/T-01-report-a-problem.md "Rules").
 * No product logic implemented yet.
 */
export function tenantOnlyPlaceholder(): string {
  return "tenant-only";
}
